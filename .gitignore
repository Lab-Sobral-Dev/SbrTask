# Claude Code local settings (machine-specific)
.claude/

# Playwright MCP artifacts
.playwright-mcp/

# Superpowers brainstorm mockups (sessão local)
.superpowers/

# Git worktrees isolados de sessão (superpowers:using-git-worktrees)
.worktrees/

# Env files — nunca commitar credenciais
.env
.env.*
!.env.example
*/.env
*/.env.*
!*/.env.example

# OS
.DS_Store
Thumbs.db
