# Backend API base URL — baked into the Vite bundle at build time.
# Development: http://localhost:3001/api
# Production:  https://sbrtask.laboratoriosobral.com/api
# The /api suffix is required (socket URL is derived by stripping it).
VITE_API_URL=https://sbrtask.laboratoriosobral.com/api
