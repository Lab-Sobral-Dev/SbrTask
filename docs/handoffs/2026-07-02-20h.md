# Handoff — 2026-07-02 20h46

## Estado em voo

Migration pendente do pivot institucional **gerada, aplicada e verificada** — mas o
commit **não subiu pro remoto** por falta de escopo no token (ver guard-rails).

Branch: `feat/institutional-pivot` (default do repo é `master`, não `main`).
Local: `C:\AMILCAR-CONSTELATTION\satelites\SbrTask`.

## O que foi feito nesta sessão

1. **Diagnóstico do drift schema↔migrations.** O `schema.prisma` do pivot tinha
   `FieldTask`, `FieldTaskSession`, `XpTransaction`, `CommissionCycle`,
   `CommissionConfig` + `telegramChatId`/`GitHubEvent`, mas o diretório de migrations
   só ia até `20260423180238_institutional_auth`. Código de XP/commission referenciava
   tabelas sem migration correspondente — branch não subia em DB limpo.

2. **Migration gerada:** `20260702231905_feat_xp_commission_system` (commit local
   `e585dea`). Cria as 6 tabelas + índices + FKs, torna `adUsername` NOT NULL, dropa
   `User_email_key`, adiciona `telegramChatId`. Aplicada contra Postgres 16 real →
   `prisma migrate status` = "Database schema is up to date!". 3 migrations, todas ok.

3. **Ambiente de dev subido do zero** (estava tudo down): WSL2 destravado, Docker
   Engine iniciado no WSL, Postgres efêmero em container. Migration rodada via
   container `node:20` (ver guard-rails sobre por que não dá pra usar o node do host).

## Guard-rails ativos

- **PUSH BLOQUEADO — token sem `contents:write`.** A conta `paulinett1508-dev` é
  colaboradora com push em `Lab-Sobral-Dev/SbrTask`, MAS o fine-grained PAT desta
  máquina só tem `metadata=read` neste repo (`x-accepted-github-permissions: metadata=read`).
  `git push` e push via token do `gh` dão o mesmo 403. **3 commits presos localmente**
  (`e585dea` desta sessão + `9bfdc20`, `115e311` de 01/07). Sem chave SSH registrada
  (`git@github.com` → publickey denied). **Correção (só o usuário):** editar o PAT em
  github.com/settings/tokens → incluir o repo + **Contents: Read and write** (pode
  exigir aprovação do owner da org). Alternativas: chave SSH ou classic PAT com `repo`.
  Enquanto não subir, **THEO fica cego pra esse trabalho** (issue_poller lê remoto).

- **node/npm dentro do WSL resolvem pro Windows via PATH interop** → `EPERM` em path
  UNC `\\wsl.localhost\...`. Para rodar prisma/npm no Linux, usar container
  (`docker run node:20 ...`), NÃO `npm` direto no WSL.

- **Postgres do compose binda `127.0.0.1:5434`** — o WSL2 NÃO encaminha binds em
  127.0.0.1 pro localhost do Windows (só encaminha `0.0.0.0`). Prisma rodou de dentro
  do WSL/container, não do Windows. Container efêmero usado: `sbrtask-pg-tmp` (pode ter
  morrido junto com a VM; recriar se precisar).

- **WSL2 instável nesta máquina** — `CreateVm` timeout `0x800705b4` recorrente. Fix que
  funcionou: PowerShell admin → `Restart-Service vmcompute, LxssManager -Force`, depois
  segurar a VM viva com keepalive (`wsl ... sleep 1800`) enquanto trabalha.

- **`.claude/settings.local.json` modificado** (não commitado) — config local, ignorar.

## Issues abertas relevantes

- #4 Tela de Ranking, #5 Logo, #6 Área de perfil — todas de abril, **pré-pivot**.
  Ranking/avatar foram REMOVIDOS no pivot institucional → provavelmente obsoletas,
  avaliar fechamento.
- PR #2 (`feat/zod-validation-tasks`) aberto desde abril — avaliar merge/descarte.
- Branch `feat/brutal-system-wide` (design "brutal" antigo, parada em 22/04) — candidata a limpeza.

## Pendências a virar issue (quando token permitir escrita — THEO precisa indexar)

1. **[db] Aplicar migration em produção (Zion) no deploy.** Atenção: `adUsername SET
   NOT NULL` e `DROP INDEX User_email_key` falham se houver dado legado incompatível.
2. **[deploy] Reconciliar 5 commits de embassy da `master` no pivot** (`e8af99f`,
   `a4145dd`, `336cb2d` + 2) — pivot está 5 atrás da master.
3. **[plan] Escrever Plan 2** (Kanban board + rebuild da UI) — citado no Plan 1, nunca escrito.
4. **[test] Smoke test completo do login institucional** (Plan 1 Task 17) — nunca rodado;
   exige backend+frontend up e LDAP (192.86.221.218) alcançável.
5. **[chore] Limpeza:** fechar issues obsoletas #4/#5/#6, resolver PR #2, avaliar branch brutal.

## Próxima ação recomendada

1. Usuário ajusta o token (Contents: write) → `git push origin feat/institutional-pivot`
   (3 commits sobem).
2. Depois: abrir as 5 issues acima no repo para o THEO indexar.
3. Reconciliar embassy da master antes de qualquer merge do pivot.

## Decisões desta sessão

- Migration gerada com `prisma migrate dev` num container `node:20` na rede do host
  (não no host Windows) — decisão forçada pelo PATH interop + port-forwarding do WSL2.
  A migration em si é padrão/limpa; a complexidade foi toda ambiental, não de schema.
- Não removi o container `sbrtask-pg-tmp` nem o dir `/tmp/sbrtask-mig` no WSL — efêmeros,
  somem quando a VM/Docker cai. Sem impacto no repo.
