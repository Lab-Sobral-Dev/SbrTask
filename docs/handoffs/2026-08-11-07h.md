# Handoff — 2026-08-11 07h52

## Estado em voo

Sessão curta, sem mudança de código. Dois assuntos abertos, nenhum concluído:

1. **Checklist "mínimo pra começar um projeto"** — usuário pediu pra documentar um
   checklist de onboarding pros devs. Discussão nunca aconteceu de fato: o pedido
   seguinte (analisar post do Instagram) tomou o resto da sessão em troubleshooting
   de ferramenta. Virou issue #14 (ver abaixo) — próxima sessão retoma direto no
   debate/conteúdo do checklist.
2. **Análise de post do Instagram** (`https://www.instagram.com/p/Db111hVkTig/`,
   uso declarado: curadoria/aprendizado) — bloqueada por ausência de ferramenta de
   browser funcional nesta máquina. Usuário estava reiniciando o Claude Code por
   conta própria pra habilitar `claude-in-chrome` quando a sessão foi encerrada.

## Estado do router (para retomada)

- tier_final: barato (sessão foi só troubleshooting + criação de issue/handoff)
- fase_final: explore
- debug_travado: 0 (o bloqueio de browser não é um bug de código, é ferramenta indisponível)
- arquivos_tocados: 0 (nenhum arquivo de código tocado)
- risco_corrente: baixo

## Issues abertas relevantes

- **#14 [docs] Checklist de mínimo para começar um projeto (onboarding de devs)** —
  criada nesta sessão. Retomar o debate com o usuário sobre o conteúdo antes de escrever.
- #11 [comissionamento] risco de identidade duplicada por dev
- #10 Ownership do código do THEO (`theo/`) hospedado neste repo
- #4, #5, #6 — pré-pivot (Ranking/Logo/Perfil), candidatas a fechamento desde o
  handoff de 2026-07-02 (ver `docs/handoffs/2026-07-02-20h.md`), ainda não avaliadas.

## Guard-rails ativos

- **Browser automation não funciona nesta máquina de dev out-of-the-box** (ver
  memória de projeto salva: `env_no_chrome_browser_tools.md`):
  - `chrome-devtools-mcp` falha: sem binário Chrome em `/opt/google/chrome/chrome`.
  - `claude-in-chrome` está instalado mas suas ferramentas MCP não aparecem
    mid-sessão só com `/chrome` — precisa reiniciar o processo do Claude Code
    inteiro pro prompt de habilitação surgir. Antes de tentar qualquer tarefa que
    dependa de navegador (skill de Instagram, QA visual, chrome-devtools debug),
    checar `ToolSearch` por `mcp__claude-in-chrome__*` primeiro; se vazio, não
    insistir — perguntar ao usuário se já reiniciou.
  - `docker` (comando) não encontrado neste shell — diferente do host Windows/WSL2
    descrito no handoff de 07/02. Não assumir que os workarounds de WSL2/Docker
    daquele handoff se aplicam a esta sessão sem confirmar o ambiente atual.
- Auditoria de overengineering (`docs/debt-ledger.md` nunca existiu neste repo)
  disparada em background nesta sessão via subagent — pode ainda estar rodando
  ou já ter terminado quando a próxima sessão abrir. Conferir `docs/debt-ledger.md`;
  se existir, é o primeiro relatório desse tipo pro repo, revisar achados antes de
  agir sobre eles (a skill só relata, não corrige sozinha).
- `.claude/settings.local.json` modificado (não commitado) — config local, ignorar,
  mesma observação do handoff anterior.

## Próxima ação recomendada

1. Retomar o debate do checklist de onboarding (#14) com o usuário — é o assunto
   que ele trouxe primeiro nesta sessão e não chegou a ser discutido.
2. Se o usuário confirmar que reiniciou o Claude Code e habilitou `claude-in-chrome`,
   retomar a análise do post do Instagram (curadoria/aprendizado) antes do checklist,
   se ele preferir nessa ordem.
3. Conferir `docs/debt-ledger.md` (auditoria rodou em background) e decidir com o
   usuário o que vira issue vs. o que fica registrado como trade-off aceito.

## Decisões desta sessão

- Nenhuma decisão arquitetural. Único incidente operacional: uma issue foi criada
  por engano no repo `paulinett1508-dev/agnostic-core` (cwd da sessão tinha
  ficado dentro do submódulo `.agnostic-core` por um `cd` anterior) — fechada lá
  e recriada corretamente em `Lab-Sobral-Dev/SbrTask#14`. Lição operacional: depois
  de qualquer `cd` para dentro do submódulo, confirmar `pwd`/`gh repo view` antes
  de rodar `gh issue create` sem `--repo` explícito.
