# Handoff — 2026-08-11 21h59

## Estado em voo

Nada em voo — sessão longa (do "olhe esse post do Instagram" até aqui) terminou com
o gate de aprovação de Task 100% implementado, revisado e com a branch parada
aguardando decisão do usuário sobre merge/PR.

Fluxo completo desta sessão: análise de um post do Instagram (@brunoguzela, curadoria/
aprendizado) → brainstorm → spec (`docs/superpowers/specs/2026-08-11-gate-aprovacao-task-design.md`)
→ plano de 11 tasks (`docs/superpowers/plans/2026-08-11-gate-aprovacao-task.md`) →
execução subagent-driven (worktree `.worktrees/feat-task-checklist-gate`, branch
`feat/task-checklist-gate`) → revisão final de branch inteira → 1 fix wave → re-review
limpo → `/fecharsessao`.

## Estado do router (para retomada)

- tier_final: médio (mix de haiku/sonnet/opus conforme complexidade de cada task/review)
- fase_final: operate (implementação + revisão concluídas, sessão encerrando)
- debug_travado: 0 (dois incidentes de git durante a execução — revert acidental na Task 6,
  commit na branch errada na Task 7 — ambos resolvidos na hora, sem ficar travado)
- arquivos_tocados: ~20 (backend: schema, controller, routes, constants, validators; frontend:
  componente, lib, 2 páginas novas + roteador)
- risco_corrente: baixo (revisão final limpa, sem achado crítico/important em aberto)

## Issues abertas relevantes

- **#14** — `[docs] Checklist de mínimo para começar um projeto` — pedido original desta
  sessão (onboarding de dev), nunca implementado — a conversa divergiu pro gate de
  aprovação de Task (escopo relacionado mas diferente: qualidade por tarefa, não
  onboarding de repo). Segue aberta, sem trabalho feito nela especificamente.
- **#15** — taskController credita XP direto, bypassando `awardXp()` — legado, não tocado
  nesta sessão (mencionado na revisão final como lembrete, não como escopo).
- **#16** — Tasks.tsx com 3 responsabilidades — **corrigido** na branch pendente (Task 9),
  comentário já postado, issue fica aberta até o merge.
- **#17** — deps `@dicebear/*` sem uso — não tocado.
- **#18** — `npm run build` falha em commission/fieldTask controllers — não tocado (fora de
  escopo, documentado); comentário adicional postado sobre `scripts/create-admin.ts` ter
  saído do typecheck.
- **#19** (nova) — gaps de UX/consistência de baixa severidade no gate (stats sem filtro,
  task rejeitada invisível na UI, resubmitTask sem guard atômico, mutations sem onError,
  falta teste de paridade dos 2 catálogos).
- **#20** (nova) — hardening de concorrência: emit de XP antes do commit da transação
  (`awardXp`, 3 call sites), TOCTOU race em `updateChecklist`.

## Guard-rails ativos

- **Branch `feat/task-checklist-gate` NÃO foi pushed** — decisão explícita do usuário
  ("deixar como está"). Não pushar/mergear sem perguntar de novo na próxima sessão.
- **Postgres local é frágil** — já caiu uma vez no meio desta sessão porque outro projeto
  reiniciou o Docker. Antes de assumir que o banco está acessível: `sudo docker ps` (meu
  usuário não está no grupo `docker` nesta sessão de trabalho — precisa `sudo` ou nova
  sessão de shell depois do `usermod -aG docker`).
- **`CLAUDE.md` documenta setup via WSL2, mas a máquina real (`labti01`) é Ubuntu 26.04
  nativo** com Docker CE instalado via apt — a seção "INFRAESTRUTURA LOCAL DE DEV" está
  desatualizada. Vale corrigir num momento oportuno (não fiz agora pra não desviar do
  fechamento desta sessão).
- **Backend `npm run dev` (ts-node) não sobe** — aborta no typecheck por causa dos erros
  pré-existentes da #18, antes de bindar a porta. Usar `npm run build && node dist/index.js`
  pra testar contra um servidor real.
- **`backend/dist/` está tracked no git** (raro, mas é assim neste repo) — sempre
  `git checkout -- backend/dist && git clean -fd backend/dist` depois de qualquer build
  local, antes de commitar.
- **Admins não são comissionados neste sistema** — se alguém reabrir a discussão sobre
  "admin pode se autoaprovar e ganhar XP", isso já foi avaliado e aceito como não-risco
  (decisão do usuário, registrada na sessão): XP dessa etapa só vai pro criador da task,
  que é sempre admin, e admin não participa das faixas de comissão.

## Próxima ação recomendada

Nenhuma ação obrigatória. Se o usuário decidir mergear:
```
cd /mnt/dados/AMILCAR-CONSTELATTION/fragatas/SbrTask
git checkout feat/institutional-pivot && git pull
git merge feat/task-checklist-gate
# rodar backend build + frontend test pra confirmar
git push origin feat/institutional-pivot
```
Depois do merge: fechar `#16` (comentário já postado, só falta o `gh issue close`).

## Decisões desta sessão

- Checklist de 11 itens do gate (4 travados sem XP: segredos, testes automatizados,
  auditoria de segurança, HTTPS/TLS; 7 opcionais com XP 10-35 conforme peso/criticidade)
  inspirado num carrossel do Instagram (@brunoguzela) — curadoria/aprendizado, ideias
  reescritas com palavras próprias, sem reprodução do post original em nenhum artefato.
- Gate vive dentro do próprio `Task` (`approvalStatus`), sem entidade `Project` nova.
- Só admin cria e aprova Task — dois passos (criar preenchendo checklist, aprovar depois,
  podendo ser outro admin), sem abrir criação pra usuário comum.
- Aprovação fica pendente numa fila pra revisão posterior, não instantânea na mesma tela.
- Rejeição volta pra edição (checklist editável + reenvio), não arquiva a task.
- Prompt cross-repo enviado pra sessão do gestao-sbr (módulo TI) resultou em design próprio
  lá, sem acoplamento de código entre os dois repos — comentário registrado na #14.
- Máquina de dev deste projeto (`labti01`) é Ubuntu 26.04 nativo, não WSL2.
